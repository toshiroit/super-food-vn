import { ContainerProps } from "src/types/container/container";

const Container = ({ children }: ContainerProps) => {
  return <div className="container-web-4868kwr">{children}</div>;
};
export default Container;
